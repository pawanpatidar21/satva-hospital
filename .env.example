# Sattva Clinic – Frontend (deployment-ready)
# Copy to .env for local overrides. App works without any variables (uses localStorage).

# Optional: disable source maps for smaller production build
# GENERATE_SOURCEMAP=false

# Optional: any REACT_APP_* variables if you add them later
# REACT_APP_SITE_NAME=Sattva Clinic

# Optional: custom encryption key for patient data in localStorage (use a strong random string in production)
# REACT_APP_ENCRYPTION_KEY=your-secure-32-char-key-here
